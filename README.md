# UrbanStay: прогноз отмены бронирования

Модель предсказывает вероятность отмены брони и переводит прогноз в деньги через метрику
Incremental Revenue (IR). Проект выполнен на данных PostgreSQL сети отелей UrbanStay:
`hotel_bookings` (35 341 бронь) и `hotel_reviews` (25 177 отзывов).

**Результат на тестовой выборке:** доля отмен 28.79% -> 8.90%, загрузка 71.21% -> 74.56%,
доход +36.40% (61.20 млн руб. на 6 147 броней, 9 957 руб. на бронь). Две цели заказчика
из трёх выполнены, причём по доле отмен - с запасом на статистическую погрешность
(верхняя граница 95% интервала 9.61% при лимите 10%). По приросту дохода получено 36.40%
против целевых 50%.

## Состав репозитория

```
SPRINT_18/
├── bbab3c3f-71b2-439b-b484-121cdb98ce67.ipynb  тетрадь проекта
├── feature_pipeline.py                          очистка, объединение, признаки, бизнес-метрики
├── requirements.txt                             точные версии окружения
├── README.md
└── artifacts/
    ├── feature_pipeline.py                      копия модуля рядом с моделью
    ├── model.joblib                             Pipeline (препроцессор + откалиброванный CatBoost) и порог
    └── model_meta.json                          гиперпараметры, оба порога, метрики, список колонок
```

## Установка

```bash
python3.9 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

На JupyterHub Практикума первая ячейка тетради сама доустанавливает недостающие пакеты
(`psycopg2-binary`, `optuna`, `lightgbm`, `catboost`, `shap`, `numba`) и при необходимости
понижает SQLAlchemy до ветки 1.4: `pandas 1.2` несовместим с SQLAlchemy 2.x.

## Инференс на новых данных

```python
import joblib
import pandas as pd
import feature_pipeline as fp

artifact = joblib.load("artifacts/model.joblib")
pipeline = artifact["pipeline"]
threshold = artifact["threshold"]          # рабочий порог, не 0.5
with_demand = artifact["with_demand"]      # включены ли признаки спроса

bookings = fp.clean_bookings(new_bookings_raw)   # сырая выгрузка hotel_bookings
reviews = fp.clean_reviews(reviews_raw)          # сырая выгрузка hotel_reviews
merged = fp.add_demand_features(fp.join_reviews(bookings, reviews))
features = fp.build_features(merged, with_demand=with_demand)

probability = pipeline.predict_proba(features)[:, 1]
risky = probability >= threshold                 # брони, по которым стоит принимать меры
```

## Пороги и что они значат

- **0.327 - рабочий порог**, сохранён в артефакте. Это максимум IR при ограничении на долю
  отмен, ужесточённом на две стандартные ошибки (9.23% вместо 10%). Оптимум с ограничением
  ровно в 10% всегда ложится на границу, а доля отмен оценена по выборке со стандартной
  ошибкой 0.38 п.п. - без запаса цель провалилась бы на новых данных примерно в половине случаев.
- **0.340 - максимум IR ровно на границе 10%.** Даёт +0.32 п.п. относительного IR, но доля
  отмен 9.99% - запас меньше одной брони из 6 147.
- **0.370 - безусловный максимум IR.** Даёт +1.87 п.п. прироста дохода, но доля отмен
  вырастает до 11.26%, то есть цель по отменам не выполняется.
- **0.395 - порог безубыточности**, следует прямо из матрицы стоимостей:
  `(AvgRev + CostFP) / (PerRebooking + LostRev + AvgRev + CostFP)`. Лежит выше эмпирического
  максимума, потому что калибровка не идеальна.

Экономические параметры (`AvgRev`, `CostFP`, `LostRev`, `PerRebooking`) заданы константами
в `feature_pipeline.py`. При их пересмотре порог нужно пересчитать функцией `fp.threshold_scan`
на свежей калибровочной выборке.

## Ограничения данных

- **Отзывы нельзя связывать с бронями по клиенту.** Отзыв есть у всех состоявшихся броней и
  ни у одной отменённой, поэтому связь через `booking_id` даёт признак, который однозначно
  определяет целевую переменную. Объединение выполняется только по дате: каждой брони достаётся
  последний отзыв, оставленный строго до её оформления.
- Из-за этого текстовые признаки почти не работают: одна и та же запись достаётся всем броням
  одного дня (15.1 брони на отзыв, в признаки попали 2 033 текста из 14 153), и все 60 TF-IDF
  колонок дают 3.36% вклада по SHAP.
- Номерного фонда в выгрузке нет, поэтому загрузка считается от числа броней (одна бронь -
  один номеро-период). Уровни загрузки нельзя сравнивать с плановыми 80% заказчика напрямую -
  корректно сравнивать динамику, на которую это допущение не влияет.
- Качество модели упирается в ROC-AUC около 0.81. Это проверено четырьмя гипотезами (признаки
  спроса, нативные категории CatBoost, блендинг, расширенный поиск Optuna) - ни одна прироста
  не дала. Дальнейший рост требует новых данных: `customer_id` в таблице броней и условий тарифа.
