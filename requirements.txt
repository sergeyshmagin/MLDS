# Окружение JupyterHub Практикума (Python 3.9.5) - тетрадь проверена ровно на этих версиях.
# Первые пять пакетов уже стоят на платформе, менять их не нужно.
numpy==1.21.1
pandas==1.2.4
scikit-learn==0.24.1
matplotlib==3.3.4
seaborn==0.11.1

# Доустанавливаются первой ячейкой тетради:
optuna
imbalanced-learn==0.8.1   # последняя ветка, совместимая с scikit-learn 0.24
mlxtend==0.23.4           # ставится с --no-deps: нужен только GroupTimeSeriesSplit
joblib==1.0.1
